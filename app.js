const storeKey='kita-kidventure-state';
const state=JSON.parse(localStorage.getItem(storeKey)||'{"favorites":[],"done":[],"checks":{}}');
const save=()=>localStorage.setItem(storeKey,JSON.stringify(state));
document.querySelectorAll('[data-favorite]').forEach(btn=>{const id=btn.dataset.favorite;if(state.favorites.includes(id)){btn.classList.add('on');btn.textContent='♥ Saved';}btn.addEventListener('click',()=>{state.favorites=state.favorites.includes(id)?state.favorites.filter(x=>x!==id):[...state.favorites,id];save();btn.classList.toggle('on');btn.textContent=btn.classList.contains('on')?'♥ Saved':'♡ Save';});});
document.querySelectorAll('[data-done]').forEach(btn=>{const id=btn.dataset.done;if(state.done.includes(id)){btn.classList.add('done');btn.textContent='✓ Done';}btn.addEventListener('click',()=>{state.done=state.done.includes(id)?state.done.filter(x=>x!==id):[...state.done,id];save();btn.classList.toggle('done');btn.textContent=btn.classList.contains('done')?'✓ Done':'Mark done';});});
document.querySelectorAll('[data-check]').forEach(input=>{const id=input.dataset.check;input.checked=!!state.checks[id];input.addEventListener('change',()=>{state.checks[id]=input.checked;save();});});
const filterBtns=document.querySelectorAll('[data-filter]');filterBtns.forEach(b=>b.addEventListener('click',()=>{filterBtns.forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;document.querySelectorAll('[data-category]').forEach(c=>c.classList.toggle('hidden',f!=='all'&&!c.dataset.category.includes(f)));}));
const search=document.querySelector('#activity-search');if(search)search.addEventListener('input',()=>{const q=search.value.toLowerCase();document.querySelectorAll('[data-search]').forEach(c=>c.classList.toggle('hidden',!c.dataset.search.toLowerCase().includes(q)));});
